"use client";
import Link from "next/link";
import { useActionState } from "react";
import { login } from "@/app/actions";
import { AppLogo } from "@/components/app-logo";
export default function Login() { const [state, action, pending] = useActionState(login, null); return <main className="grid min-h-screen place-items-center p-4"><form action={action} className="surface w-full max-w-sm rounded-[1.7rem] p-6 sm:p-8"><div className="mb-7 text-center"><AppLogo size="medium" className="mx-auto mb-5" /><h1 className="app-title text-2xl font-bold text-white">Admin Login</h1><p className="mt-1 text-sm text-slate-400">Manage all InterviewDesk schedules.</p></div><label className="mb-4 block text-sm font-medium text-slate-300">Admin username<input className="mt-1.5" name="email" autoComplete="username" required /></label><label className="mb-5 block text-sm font-medium text-slate-300">Password<input className="mt-1.5" name="password" type="password" autoComplete="current-password" required /></label>{state?.error && <p className="mb-3 text-sm text-red-400">{state.error}</p>}<button disabled={pending} className="w-full rounded-xl bg-gradient-to-r from-violet-600 via-indigo-500 to-sky-500 px-4 py-3 font-semibold text-white shadow-lg shadow-violet-950/30 hover:brightness-110 disabled:opacity-60">{pending ? "Signing in…" : "Admin sign in"}</button><p className="mt-5 text-center text-sm"><Link className="text-violet-300" href="/">Back to options</Link></p></form></main>; }
