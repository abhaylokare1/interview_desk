"use client";
import Link from "next/link";
import { useActionState } from "react";
import { login } from "@/app/actions";
export default function Login() { const [state, action, pending] = useActionState(login, null); return <main className="grid min-h-screen place-items-center p-4"><form action={action} className="w-full max-w-sm rounded-2xl border border-white/10 bg-[#111522]/90 p-6 shadow-2xl shadow-black/30 sm:p-8"><div className="mb-7 text-center"><span className="mx-auto mb-4 grid size-12 place-items-center rounded-2xl bg-gradient-to-br from-violet-500 to-sky-500 text-xl font-bold text-white">I</span><h1 className="text-2xl font-bold text-white">Admin Login</h1><p className="mt-1 text-sm text-slate-400">Manage all InterviewDesk schedules.</p></div><label className="mb-4 block text-sm font-medium text-slate-300">Admin username<input className="mt-1.5" name="email" autoComplete="username" required /></label><label className="mb-5 block text-sm font-medium text-slate-300">Password<input className="mt-1.5" name="password" type="password" autoComplete="current-password" required /></label>{state?.error && <p className="mb-3 text-sm text-red-400">{state.error}</p>}<button disabled={pending} className="w-full rounded-xl bg-gradient-to-r from-violet-600 to-indigo-500 px-4 py-3 font-semibold text-white disabled:opacity-60">{pending ? "Signing in…" : "Admin sign in"}</button><p className="mt-5 text-center text-sm"><Link className="text-violet-300" href="/">Back to options</Link></p></form></main>; }
