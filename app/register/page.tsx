"use client";
import Link from "next/link";
import { useActionState } from "react";
import { register } from "@/app/actions";
export default function Register() { const [state, action, pending] = useActionState(register, null); return <main className="grid min-h-screen place-items-center p-4"><form action={action} className="w-full max-w-sm rounded-2xl border border-white/10 bg-[#111522]/90 p-6 shadow-2xl shadow-black/30 sm:p-8"><div className="mb-7 text-center"><span className="mx-auto mb-4 grid size-12 place-items-center rounded-2xl bg-gradient-to-br from-violet-500 to-sky-500 text-xl font-bold text-white">S</span><h1 className="text-2xl font-bold text-white">Create your account</h1><p className="mt-1 text-sm text-slate-400">Start scheduling interviews in minutes.</p></div><label className="mb-4 block text-sm font-medium text-slate-300">Name<input className="mt-1.5" name="name" autoComplete="name" required /></label><label className="mb-4 block text-sm font-medium text-slate-300">Email<input className="mt-1.5" name="email" type="email" autoComplete="email" required /></label><label className="mb-4 block text-sm font-medium text-slate-300">Contact number <span className="text-slate-500">(optional)</span><input className="mt-1.5" name="contactNumber" type="tel" autoComplete="tel" /></label><label className="mb-5 block text-sm font-medium text-slate-300">Password <span className="text-slate-500">(8+ characters)</span><input className="mt-1.5" name="password" type="password" autoComplete="new-password" required minLength={8} /></label>{state?.error && <p className="mb-3 text-sm text-red-400">{state.error}</p>}<button disabled={pending} className="w-full rounded-xl bg-gradient-to-r from-violet-600 to-indigo-500 px-4 py-3 font-semibold text-white disabled:opacity-60">{pending ? "Creating account…" : "Create account"}</button><p className="mt-5 text-center text-sm text-slate-400">Already have an account? <Link className="font-semibold text-violet-300" href="/login">Sign in</Link></p></form></main>; }
