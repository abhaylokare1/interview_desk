import Link from "next/link";
import { redirect } from "next/navigation";
import { getSession } from "@/lib/auth";
export const dynamic = "force-dynamic";
export default async function Home() { const session = await getSession(); if (session?.role === "admin") redirect("/dashboard"); return <main className="grid min-h-screen place-items-center p-4"><section className="w-full max-w-xl rounded-3xl border border-white/10 bg-[#111522]/90 p-7 text-center shadow-2xl shadow-black/30 sm:p-10"><span className="mx-auto grid size-14 place-items-center rounded-2xl bg-gradient-to-br from-violet-500 to-sky-500 text-2xl font-bold text-white">I</span><p className="mt-6 text-sm font-semibold tracking-widest text-violet-300">INTERVIEWDESK</p><h1 className="mt-2 text-3xl font-bold tracking-tight text-white sm:text-4xl">Schedule interviews without the extra steps.</h1><p className="mx-auto mt-3 max-w-md text-slate-400">Students can schedule directly. Administrators can sign in to manage every interview record.</p><div className="mt-8 grid gap-3 sm:grid-cols-2"><Link href="/schedule" className="rounded-xl bg-gradient-to-r from-violet-600 to-indigo-500 px-5 py-3 font-semibold text-white">Schedule Interview</Link><Link href="/login" className="rounded-xl border border-white/15 px-5 py-3 font-semibold text-slate-200 hover:bg-white/5">Admin Login</Link></div></section></main>; }
