import Link from "next/link";
import { redirect } from "next/navigation";
import { getSession } from "@/lib/auth";
import { AppLogo } from "@/components/app-logo";
export const dynamic = "force-dynamic";
export default async function Home() { const session = await getSession(); if (session?.role === "admin") redirect("/dashboard"); return <main className="grid min-h-screen place-items-center overflow-hidden p-4"><section className="surface relative w-full max-w-xl overflow-hidden rounded-[2rem] p-7 text-center sm:p-11"><div className="absolute inset-x-0 top-0 h-px bg-gradient-to-r from-transparent via-violet-300/70 to-transparent" /><AppLogo size="large" className="mx-auto" /><p className="eyebrow mt-7">INTERVIEWDESK</p><h1 className="app-title mx-auto mt-3 max-w-md text-4xl font-bold text-white sm:text-5xl">Plan your next interview with clarity.</h1><p className="mx-auto mt-4 max-w-md text-[15px] leading-6 text-slate-400">Schedule your interview in seconds, or sign in to manage every record in one place.</p><div className="mt-9 grid gap-3 sm:grid-cols-2"><Link href="/schedule" className="rounded-2xl bg-gradient-to-r from-violet-600 via-indigo-500 to-sky-500 px-5 py-3.5 font-semibold text-white shadow-lg shadow-violet-950/35 transition hover:-translate-y-0.5 hover:brightness-110">Schedule Interview</Link><Link href="/login" className="rounded-2xl border border-white/12 bg-white/[.035] px-5 py-3.5 font-semibold text-slate-100 transition hover:-translate-y-0.5 hover:border-white/25 hover:bg-white/[.07]">Admin Login</Link></div></section></main>; }
