import ExcelJS from "exceljs";
import { getSession } from "@/lib/auth";
import { getInterviews } from "@/lib/db";
export const runtime = "nodejs";
export const dynamic = "force-dynamic";
export async function GET() { const session = await getSession(); if (!session || session.role !== "admin") return new Response("Unauthorized", { status: 401 }); const interviews = await getInterviews(session); const workbook = new ExcelJS.Workbook(); const sheet = workbook.addWorksheet("Interviews"); sheet.columns = [{ header: "Student Name", key: "studentName", width: 24 }, { header: "Years of Experience", key: "yearsOfExperience", width: 20 }, { header: "Experience Type", key: "experienceType", width: 22 }, { header: "Interview Date", key: "interviewDate", width: 16 }, { header: "From Time", key: "fromTime", width: 14 }, { header: "To Time", key: "toTime", width: 14 }, { header: "Technology", key: "technology", width: 24 }, { header: "Company Name", key: "companyName", width: 24 }, { header: "Interview Type", key: "interviewType", width: 16 }, { header: "Contact Number", key: "contactNumber", width: 18 }, { header: "Interview Round", key: "interviewRound", width: 18 }, { header: "Status", key: "status", width: 15 }]; sheet.addRows(interviews); sheet.getRow(1).font = { bold: true }; sheet.getRow(1).fill = { type: "pattern", pattern: "solid", fgColor: { argb: "DCE6F8" } }; sheet.views = [{ state: "frozen", ySplit: 1 }]; sheet.autoFilter = { from: "A1", to: "L1" }; const data = await workbook.xlsx.writeBuffer(); return new Response(data, { headers: { "Content-Type": "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", "Content-Disposition": "attachment; filename=student_interviews.xlsx" } }); }
