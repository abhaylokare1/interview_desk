import Link from "next/link";
import { logout } from "@/app/actions";
import type { Session } from "@/lib/types";
const link = "rounded-xl border border-white/10 bg-white/[.025] px-3.5 py-2 text-sm font-semibold text-slate-200 transition hover:-translate-y-0.5 hover:border-violet-400/45 hover:bg-violet-500/12 hover:text-white";
export function Nav({ session }: { session: Session }) { const admin = session.role === "admin"; return <header className="sticky top-0 z-20 border-b border-white/8 bg-[#090b14]/80 backdrop-blur-xl"><div className="mx-auto flex max-w-7xl flex-wrap items-center justify-between gap-3 px-4 py-3"><Link href="/dashboard" className="flex items-center gap-2.5 rounded-xl px-1 text-lg font-bold tracking-tight text-white"><span className="grid size-9 place-items-center rounded-xl bg-gradient-to-br from-violet-500 via-indigo-500 to-sky-500 text-sm shadow-lg shadow-violet-950/40">I</span>InterviewDesk</Link><nav className="flex w-full items-center gap-2 overflow-x-auto pb-1 sm:w-auto sm:pb-0"><Link className={link} href="/dashboard">Dashboard</Link><Link className={link} href="/interviews/new">{admin ? "Add Interview" : "Schedule Interview"}</Link><Link className={link} href="/interviews">{admin ? "All Interviews" : "My Interviews"}</Link>{admin && <a className={link} href="/api/export">Export Excel</a>}<form action={logout}><button className="rounded-xl border border-red-400/20 bg-red-500/[.035] px-3.5 py-2 text-sm font-semibold text-red-200 hover:-translate-y-0.5 hover:bg-red-500/10">Logout</button></form></nav></div></header>; }
