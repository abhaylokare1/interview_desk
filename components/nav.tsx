import Link from "next/link";
import { logout } from "@/app/actions";
import type { Session } from "@/lib/types";
const link = "rounded-lg px-3 py-2 text-sm text-slate-300 transition hover:bg-white/8 hover:text-white";
export function Nav({ session }: { session: Session }) { const admin = session.role === "admin"; return <header className="sticky top-0 z-20 border-b border-white/8 bg-[#0b0e19]/85 backdrop-blur-xl"><div className="mx-auto flex max-w-7xl flex-wrap items-center justify-between gap-2 px-4 py-3 sm:gap-4"><Link href="/dashboard" className="flex items-center gap-2 text-lg font-bold tracking-tight text-white"><span className="grid size-8 place-items-center rounded-lg bg-gradient-to-br from-violet-500 to-sky-500 text-sm">I</span>InterviewDesk</Link><nav className="flex w-full items-center gap-1 overflow-x-auto pb-1 sm:w-auto sm:pb-0"><Link className={link} href="/dashboard">Dashboard</Link><Link className={link} href="/interviews/new">{admin ? "Add Interview" : "Schedule Interview"}</Link><Link className={link} href="/interviews">{admin ? "All Interviews" : "My Interviews"}</Link>{admin && <a className={link} href="/api/export">Export Excel</a>}<form action={logout}><button className="rounded-lg px-3 py-2 text-sm text-slate-400 hover:bg-red-500/10 hover:text-red-300">Logout</button></form></nav></div></header>; }
